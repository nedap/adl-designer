{>main}
<ul class="nav nav-tabs">
    <li role="presentation" class="active">
        <a href="#{{panel_id}}_authoring" data-toggle="tab">Authoring</a></li>
    <li role="presentation"><a href="#{{panel_id}}_language" data-toggle="tab">Language</a></li>
    <li role="presentation"><a href="#{{panel_id}}_details" data-toggle="tab">Details</a></li>
</ul>
<div class="tab-content">
    <div role="tabpanel" class="tab-pane fade active in"
         id="{{panel_id}}_authoring">
        Authoring
    </div>
    <div role="tabpanel" class="tab-pane fade in"
         id="{{panel_id}}_language">
        Language
    </div>
    <div role="tabpanel" class="tab-pane fade in"
         id="{{panel_id}}_details">
        Details
    </div>
</div>

{>authoring}
<div class="row">
    <div class="col-sm-6">
        <label>Original Author</label>

        <div id="{{panel_id}}_original_author">
        </div>
    </div>
    <div class="col-sm-6">
        <label>Contributors</label>

        <div id="{{panel_id}}_contributors">
        </div>
    </div>
</div>

{>language}
<div class="form-horizontal row">
    <div class="form-group col-sm-6">
        <label class="col-sm-4 control-label">Original Language</label>

        <div class="col-sm-6">
            <p class="form-control-static" id="{{panel_id}}_original_language">{{original_language}}</p>
        </div>
    </div>
    <div class="form-group col-sm-6">
        <label for="{{panel_id}}_translations" class="col-sm-6 control-label">Translations</label>

        <div class="input-group col-sm-6">
            <select id="{{panel_id}}_translations" class="form-control input"></select>
                <span class="input-group-btn">
                    <button id="{{panel_id}}_add_translation" type="button" class="btn btn-default"
                            title="add translation">
                        <span class="glyphicon glyphicon-plus"></span>
                    </button>
                </span>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-sm-6">
        <div class="row">
            <label>Translator</label>

            <div id="{{panel_id}}_translator">
            </div>
        </div>
        <div class="row">
            <label for="{{panel_id}}_accreditation">Translator Accreditation</label>

            <input id="{{panel_id}}_accreditation" class="form-control input">
        </div>
    </div>
    <div class="col-sm-6">
        <label>Translator Other Details</label>

        <div id="{{panel_id}}_other_details">
        </div>
    </div>
</div>

{>details}
<div class="container-fluid">
    <div class="form-horizontal row">
        <div class="form-group col-sm-4">
            <label for="{{panel_id}}_language" class="col-sm-4 control-label">Language</label>

            <div class="col-sm-8">
                <select id="{{panel_id}}_language" class="form-control input"></select>
            </div>
        </div>
        <div class="form-group col-sm-4"></div>

        <div class="form-group col-sm-4">
            <label for="{{panel_id}}_lifecycle" class="col-sm-4 control-label">Lifecycle</label>

            <div class="col-sm-8">
                <input id="{{panel_id}}_lifecycle" class="form-control input">
            </div>
        </div>
    </div>
    <div class="form-horizontal row">
        <div class="form-group row">
            <label for="{{panel_id}}_purpose" class="col-sm-1 control-label">Purpose</label>

            <div class="col-sm-11">
                <textarea id="{{panel_id}}_purpose" class="form-control input" style="resize: none" rows="3"></textarea>
            </div>
        </div>
        <div class="form-group row">
            <label for="{{panel_id}}_use" class="col-sm-1 control-label">Use</label>

            <div class="col-sm-11">
                <textarea id="{{panel_id}}_use" class="form-control input" style="resize: none" rows="6"></textarea>
            </div>
        </div>
        <div class="form-group row">
            <label for="{{panel_id}}_misuse" class="col-sm-1 control-label">Misuse</label>

            <div class="col-sm-11">
                <textarea id="{{panel_id}}_misuse" class="form-control input" style="resize: none" rows="6"></textarea>
            </div>
        </div>
        <div class="form-group row">
            <label for="{{panel_id}}_copyright" class="col-sm-1 control-label">Copyright</label>

            <div class="col-sm-11">
                <input id="{{panel_id}}_copyright" class="form-control input">
            </div>
        </div>
        <div class="form-group row">
            <label for="{{panel_id}}_keywords" class="col-sm-1 control-label">Keywords</label>

            <div class="col-sm-11">
                <input id="{{panel_id}}_keywords" class="form-control input">
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-6">
            <label>Resources</label>

            <div id="{{panel_id}}_resources">
            </div>
        </div>
        <div class="col-sm-6">
            <label>Other Details</label>

            <div id="{{panel_id}}_other_details">
            </div>
        </div>

    </div>
</div>